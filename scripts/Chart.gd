class_name Chart
extends Resource

## 채보의 단일 진실 소스. 렌더도 판정도 전부 여기서 파생된다.
## 별도의 노트 타임라인을 만들지 않는다 — 두 개의 진실 소스가 있으면
## 둘이 어긋날 수 있는 버그 표면이 생기고, 그게 클론들이 "미묘하게 안 맞는" 이유다.

## 분당 박자. 0 이하면 ChartRuntime 이 빈 배열을 돌려준다.
@export var bpm: float = 120.0

## 타일별 '나갈 방향'(도). 절대각, 반시계(CCW) 양수.
## angles[0] 은 출발 타일의 나갈 방향이라 대기 계산 대상이 아니다.
@export var angles: PackedFloat32Array = PackedFloat32Array()

## 곡 시작부터 타일 0 까지의 시간(ms).
@export var start_offset_ms: float = 0.0

## 곡. 차트와 곡은 항상 같이 다녀야 하므로 여기 들고 있는다.
@export var audio: AudioStream

## 속도 변경 타일 (얼불춤의 토끼/달팽이 = SetSpeed).
##   x = 타일 인덱스 · y = 그 타일부터 적용할 배율 (chart.bpm 대비 절대값)
##   y > 1 이면 토끼(빨라짐), y < 1 이면 달팽이(느려짐).
##
## 두 배열로 나누지 않고 Vector2 하나로 두는 이유: 길이가 어긋날 수 없다.
## 배율은 '누적'이 아니라 '대체'다 — 디버깅할 때 그 타일의 배속을 바로 읽을 수 있다.
##
## 주의: 배속이 올라가면 타일 간격이 좁아지고, 판정창이 자동으로 같이 좁아진다
## (Judge.set_gaps). 그래서 3배속 구간에서도 판정창이 이웃 타일에 안 닿는다.
@export var speed_changes: PackedVector2Array = PackedVector2Array()

## Twirl 타일 (회전 방향 반전). 이 타일부터 공전이 반대로 돈다.
##
## 장식이 아니라 '경로 다양성의 유일한 수단'이다.
## 0.5박 홉은 CCW 에서 항상 -90도(우회전)라 네 번 연속이면 닫힌 사각형이 된다.
## twirl 로 CW 로 뒤집으면 같은 0.5박이 +90도(좌회전)가 되어 지그재그가 된다.
## 실제 얼불춤 채보가 타일의 25% 에 twirl 을 거는 이유가 이것이다.
@export var twirl_tiles: PackedInt32Array = PackedInt32Array()

## 고스트(자동 통과) 타일. 행성은 지나가지만 밟지 않는다 — 판정 대상이 아니다.
##
## 왜 필요한가: 2박 대기는 스윕이 정확히 360도라 기하적으로 '항상 U턴'이다
## (나갈 방향 ≡ 들어온 방향 + 180, 회전 방향과 무관 — twirl 로도 못 바꾼다).
## 채움 타일이 2박 걸음으로 이어지면 경로가 두 지점을 왕복하며 같은 자리에
## 수십 장씩 쌓인다. 2박을 1박+1박으로 쪼개고 중간 타일을 고스트로 두면
## 같은 리듬이 직선 걸음이 된다 — 탭 리듬은 그대로, 기하만 풀린다.
## 고스트는 손이 아니라 눈을 위한 타일이다.
@export var ghost_tiles: PackedInt32Array = PackedInt32Array()

## 행성 수. 2 가 기본이고, 3 이면 원작의 '삼행성' 모드다.
##
## 왜 채보마다 두나: 행성 수가 박자와 각도의 관계를 바꾼다. 도는 행성이
## 반대편(180도)이 아니라 그보다 더 돌아간 자리에서 출발하기 때문이다.
##   시작 오프셋 = (P-2) x 180 / P     (P=2 -> 0도 · P=3 -> 60도)
## 그래서 직선 타일이 2행성에서 1박, 3행성에서 2/3박이 된다.
## 같은 채보를 행성 수만 바꿔 쓰면 곡과 통째로 어긋난다 — 채보가 자기 행성 수를
## 들고 있어야 하는 이유다. (원작 변환 도구 planetamountconvert.py 도 같은 식을 쓴다:
## 변환할 때 박자 값과 BPM 에 from/to 비율을 곱해 벽시계를 보존한다.)
##
## 기본값 2 라서 기존 채보는 한 글자도 안 바뀐다 — 오프셋이 정확히 0 이다.
@export var planet_count: int = 2

## 홀드 타일. (타일 인덱스, 추가 공전 수) 쌍.
##
## 이 타일을 밟은 뒤 행성이 제자리에서 N바퀴 더 돌고, 그동안 플레이어는 키를
## 누르고 있어야 한다. 다 돌면 떼야 하고, 그 '뗌'도 판정 대상이다.
##
## 왜 한 바퀴가 2박인가: 공전 한 바퀴 = 스윕 360도 = 2박이 이 게임의 정의다
## (Chart.angles 주석). 그래서 추가 바퀴는 항상 짝수 박이고, 360도의 배수라
## **착지 지점이 안 바뀐다** — 홀드를 넣어도 경로 기하가 그대로라는 뜻이다.
##
## 지금까지 입력 동사가 '탭' 하나뿐이었다. 손맛 다양성이 제일 크게 늘어나는
## 지점이라 넣었다(원작도 DLC 로 추가했다).
@export var hold_tiles: PackedVector2Array = PackedVector2Array()

## 체크포인트 타일. 체력이 바닥나면 곡 처음이 아니라 여기서 되살아난다.
##
## 왜 필요한가: 곡이 150~180초에 타일이 500~850개다. 80% 지점에서 죽으면
## 앞의 2분을 다시 쳐야 하는데, 그건 난이도가 아니라 벌이다.
## 원작도 같은 이유로 체크포인트가 있다.
@export var checkpoint_tiles: PackedInt32Array = PackedInt32Array()

## 중간회전(midspin) 타일. 이 타일에 '도달하는' 공전은 두 행성이 역할을
## 교대하지 않고 이어서 돈다 — 공전 시작 각이 180도 달라진다.
##
##   보통     시작각 = 들어온방향 + 180   ->  나갈방향 = 들어온방향 + 180 + spin*b*180
##   중간회전  시작각 = 들어온방향        ->  나갈방향 = 들어온방향       + spin*b*180
##
## 박자 b 는 그대로인데 기하만 180도 뒤집힌다. 이게 이 게임에 없던 유일한
## '공짜 자유도'다 — 리듬이 회전 '크기'를 강제하므로 그 전에는 방향(spin)
## 하나밖에 고를 게 없었고, 짧은 홉이 이어지면 무조건 코일이 됐다:
##
##   0.25박 홉의 진행방향 변화 — 보통 225도(=-135도, 네 번이면 제자리)
##                              중간회전 45도 (완만하게 뻗는다)
##
## 실측(14곡): 이걸 넣자 경로 겹침이 1,504쌍 -> 28쌍(98% 감소).
## 착지 불변식은 그대로다 — 시작각이 어디든 끝각은 정의상 angles[i-1] 이다.
@export var midspin_tiles: PackedInt32Array = PackedInt32Array()

## 토끼/달팽이 마커를 '표시할' 속도 타일. 배속 자체는 speed_changes 가 전부다.
##
## 왜 나눴나: 원곡 오디오 채보는 타일 벽시계를 원시 전사 맵에 맞추기 위해
## 홉마다 미세 배율(전사 잡음 ±7%)이 붙는다. 그건 기계적 보정이지 플레이어가
## 반응할 '속도 변경'이 아니다 — 전부 마커로 그리면 토끼가 화면을 뒤덮는다.
## 비어 있으면 speed_changes 전부를 표시한다(구버전 채보 호환).
## [-1] 하나만 있으면 '표시할 것 없음'의 명시다(빈 배열과 구분).
@export var speed_display: PackedInt32Array = PackedInt32Array()

@export var title: String = ""


## 인스펙터에서 필드를 빠뜨렸는지 한 곳에서 확인한다.
func is_valid() -> bool:
	return audio != null and bpm > 0.0 and angles.size() >= 2


func describe() -> String:
	return "%s (bpm %.1f, %d tiles)" % [title, bpm, angles.size()]
